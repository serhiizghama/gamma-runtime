/**
 * Session interfaces — re-exported from @gamma/types.
 * This file exists for backward compatibility; prefer importing from @gamma/types directly.
 */
export type { WindowSession, CreateSessionDto } from '@gamma/types';
