{
  "name": "gamma-os-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "dev:kernel": "npm run start:dev --prefix kernel",
    "build": "npm run build --prefix web",
    "build:kernel": "npm run build --prefix kernel",
    "start:kernel": "npm run start:prod --prefix kernel"
  }
}
